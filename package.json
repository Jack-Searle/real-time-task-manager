{
  "name": "real-time-task-manager",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && mkdir -p backend/src/main/resources/static && cp -R frontend/dist/. backend/src/main/resources/static/ && ./backend/gradlew -p backend bootJar",
    "start": "java -jar backend/build/libs/backend-0.0.1-SNAPSHOT.jar",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "backend:dev": "./backend/gradlew -p backend bootRun",
    "backend:build": "./backend/gradlew -p backend bootJar"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}
